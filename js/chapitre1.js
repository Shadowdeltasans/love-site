const phrase = ["I","LOVE","YOU"] ;
let phraseIndex = 0
let letterIndex = 0


function typeWriter() {
    // si letterIndex < longueur de la phrase actuelle
        // ajouter la lettre au HTML
        // letterIndex + 1
        // setTimeout(typeWriter, 100)
    // sinon (phrase terminée)
        // letterIndex = 0
        // phraseIndex + 1
        // setTimeout(typeWriter, 1000) ← pause plus longue entre phrases
}

typeWriter() // démarrer